@model bakimonarim.Data.Urun

@{
    ViewBag.Title = "Yeni Ürün";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPageAdmin.cshtml";
}

<div class="content-wrapper">
    <section class="content-header">
        <h1><small>Ürün</small> Yeni Ekle</h1>
        <ol class="breadcrumb">
            <li><a href="@Url.Action("Index")">Ürün Listesi</a></li>
            <li>Yeni Ekle</li>
        </ol>
    </section>

    <section class="content">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Ürün Bilgileri</h3>
            </div>
            <div class="box-body">
                @using (Html.BeginForm("Create", "Urun", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                    <div class="form-group">
                        <label>Ürün Adı</label>
                        @Html.EditorFor(m => m.Adi, new { htmlAttributes = new { @class = "form-control", placeholder = "Ürün adını girin" } })
                        @Html.ValidationMessageFor(m => m.Adi, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        <label>Kategori</label>
                        @Html.DropDownList("KategoriId", (SelectList)ViewBag.Kategoriler, "-- Kategori Seçin --", new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.KategoriId, "", new { @class = "text-danger" })
                    </div>

                    <div class="form-group">
                        <label>Açıklama</label>
                        @Html.TextAreaFor(m => m.Aciklama, new { @class = "form-control", rows = "4", placeholder = "Ürün açıklaması" })
                    </div>

                    <div class="form-group">
                        <label>Ürün Resmi</label>
                        <input type="file" name="resim" class="form-control" accept="image/*" />
                    </div>

                    <div class="row">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Tarih</label>
                                @Html.EditorFor(m => m.Tarih, new { htmlAttributes = new { @class = "form-control", type = "date" } })
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Sıra</label>
                                @Html.EditorFor(m => m.Sira, new { htmlAttributes = new { @class = "form-control", placeholder = "0" } })
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Durum</label>
                                <div class="checkbox">
                                    <label>
                                        @Html.CheckBoxFor(m => m.Onay) Aktif
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-save"></i> Kaydet
                    </button>
                    <a href="@Url.Action("Index")" class="btn btn-default">
                        <i class="fa fa-arrow-left"></i> Geri Dön
                    </a>
                }
            </div>
        </div>
    </section>
</div>
